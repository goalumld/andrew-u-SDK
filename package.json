{
  "name": "lotr-sdk-project",
  "version": "0.0.2",
  "type": "module",
  "author": "Andrew Um",
  "description": "Javascript files with fetch() functions that are ready to be implemented into any project. API endpoints provided by The-One-API.\r *https://the-one-api.dev/*",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "cross-fetch": "^3.1.5"
  }
}
